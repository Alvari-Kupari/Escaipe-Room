package nz.ac.auckland.se206.controllers;

import javafx.fxml.FXML;

/** Used to manage the restart screen for when main menu button is pressed. */
public class LoadingController {

  /**
   * Initialises the controller for the loading screen. This scene will take up the whole stage and
   * an appropriate gif will also be played.
   */
  @FXML
  private void initialize() {
    System.out.println();
    System.out.println("************** Initialising ExitController **************");
  }
}
